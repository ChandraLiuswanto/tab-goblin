{
  "name": "@tab-goblin/fixture-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --root ../.. packages/fixture-site/test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
